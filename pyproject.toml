[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cross-event-prototype-verifier"
version = "0.1.0"
description = "Gait-confirmed, appearance-absorbing open-set identification with quarantined prototype memory."
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24,<3",
]

[project.optional-dependencies]
gui = [
  "opencv-python>=4.8,<5",
]
gui-qt = [
  "opencv-python>=4.8,<5",
  "PyQt6>=6.7,<7",
]
production = [
  "opencv-python>=4.8,<5",
  "torch==2.12.1+cu132",
  "torchvision==0.27.1+cu132",
  "ultralytics==8.3.163",
  "lap==0.5.13",
  "onnxruntime-gpu==1.27.0; platform_system == 'Windows'",
  "onnxruntime-gpu>=1.27,<1.28; platform_system != 'Windows'",
]

[project.scripts]
cross-event-verifier = "cross_event_verifier.cli:main"

[tool.setuptools.packages.find]
include = ["cross_event_verifier*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
