# 项目领域词汇

## 身份与步态

- **视觉身份（appearance identity）**：由 OSNet 外观匹配、人工确认或外部采集任务确定的 `P1`、`P2` 等身份标签。它表示“当前 Track 被关联到哪个身份”，不等于该身份已经具备可靠步态识别能力。
- **步态样本（gait sample）**：一段满足姿态质量和行走条件的时序片段，经 GaitGraph2 编码得到的单个步态向量。
- **步态事件（gait event）**：同一次采集会话或挑战中产生的一组步态样本。一个事件只能计数一次，不能把同一会话中的多个窗口当成多个独立事件。
- **步态原型（gait prototype）**：经过质量、稳定性和事件独立性审核后，代表一个步态事件或一个步态条件簇的向量，归属于某个视觉身份。
- **步态就绪（gait ready）**：某个视觉身份的步态原型集合已经通过数量、质量、独立性、覆盖度、内部一致性和留出验证，允许未来使用步态向量进行身份检索。

## 状态含义

- **视觉已确认**：已有可靠的 OSNet/人工身份绑定，但尚未证明步态识别可用。
- **步态学习中**：正在为已确认身份收集和审核独立步态事件。
- **步态暂可用**：已有最低数量的合格原型，但尚未完成充分的留出或开放集验证。
- **步态就绪**：通过正式就绪判定，可以将 GaitGraph2 作为主要身份检索分支。
- **步态冲突**：独立事件之间无法解释为同一人的稳定步态，暂停写入并要求复核。

## 当前实现约定

- 生产 GUI 使用 OSNet-first 流程：先通过 OSNet 的稳定外观样本确认或创建视觉身份，再允许同一绑定 Track 的 GaitGraph2 输出进入步态学习。
- `gait_enrollment_events` 按 `(identity_id, event_key)` 去重；同一 `capture_session_id` 或 `challenge_id` 的重复窗口不会增加独立步态事件数，但通过质量门的后续步态样本会继续更新该事件代表向量和对应步态原型。
- “步态暂可用”默认需要 2 个独立事件；“步态就绪”默认需要 3 个事件、至少 2 个摄像头/视角条件，并通过 leave-one-event-out 留出和已有图库开放集检查。
- 步态样本学习默认至少需要 25 个真实有效姿态帧；GaitGraph2 的正式身份检索默认至少需要 45 个真实有效姿态帧和 0.75 的真实姿态覆盖率，固定长度重采样不增加真实观测帧数。
- 高质量外观与已绑定视觉身份明确冲突时进入“步态冲突/隔离”，GaitGraph2 不能越权改写视觉身份；未绑定 Track 也不能仅凭一个步态窗口直接绑定正式 `P`。
- 连续 Track 的 source-local candidate key 是短期 must-link；同一帧可靠 Track 试图共享同一视觉身份时形成 cannot-link 冲突。
- 一个视觉身份可以吸收不同视角的稳定 OSNet appearance prototypes；这些外观原型仍只用于视觉身份绑定和候选关联，不作为 GaitGraph2 的输入。
- 步态原型写入只保存 GaitGraph2 输出向量，不在线更新 GaitGraph2 权重；旧版步态优先 API 保留为兼容路径，不作为生产 GUI 的默认流程。
